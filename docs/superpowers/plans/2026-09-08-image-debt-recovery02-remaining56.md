# Recovery 02 remaining 56 — Execution Plan

> Execute inline. No commit, push, PR, stash operation, Knowledge expansion, or Batch10. Existing 387 accepted recovery images must remain intact.

**Goal:** Search new alternatives for the current 23 City and 33 Core POI debts without lowering entity, licensing, visual-quality or size requirements.

**Architecture:** Reuse the existing recovery and image processing pipeline with an isolated output directory. Preserve prior attempts and skip previously visually rejected source filenames. Only explicitly reviewed replacements are promoted; unresolved targets retain neutral placeholders.

**Tech stack:** Node ESM, Commons/Wikidata/Wikipedia metadata, existing Pillow processor, existing visual audit and browser verifiers.

## Execution

- [ ] Add `--output-root` to `scripts/recover-route-v2-image-debt.mjs` for isolated candidates/state and `--dry-run` to confirm selection without network/writes. Keep default behavior and product runtime unchanged.
- [ ] Recover rejected source titles from prior decisions and chosenCandidate; retain previous attempt history. Verify known rejected Dilijan/Doha files are skipped and approved entities cannot enter isolated retry.
- [ ] Derive retry QIDs from the current manifest/results (56), then run `node scripts/recover-route-v2-image-debt.mjs --profile=recovery02 --retry=<derived QIDs> --output-root=<new OS temp directory>` with the existing Python runtime. Expand exact linked categories to the existing retry limit; review canonical-source alternatives for remaining gaps.
- [ ] Visually inspect every candidate before promotion. Verify exact entity, file-level license URL, creator/attribution, decode, dimensions, size and duplicate hashes. No generated imagery or unverified search-result publication.
- [ ] Promote approved files with complete history, update current manifests/reports and visual decisions, track approved LFS assets, then regenerate the strict image baseline. Keep historical image debt 13 unchanged.
- [ ] Run affected image/provenance/report/consumer checks and targeted real-browser acceptance. Preserve existing formal hashes, route behavior, performance contract, and stash; disclose tests not rerun and any unresolved failures.
- [ ] Report recovered and remaining counts, rejection reasons, exact provenance, test results and uncommitted Git state. Stop without commit/push/PR.

## Resume checkpoint — 2026-09-09 Singapore

Initial branch/HEAD matched; 388 staged entries and stash object 2a874aa32df41285a150e79d6a8981cee2f032db verified. Existing 387 accepted image bytes and pre-turn modified text were snapshotted outside the project. Existing isolated retry completed: 26 pending candidates and 30 unresolved, not 413 approved.

- [x] Read existing plan, scripts, formal reports, isolated log/results/provenance and both contact sheets.
- [x] Inspect candidate originals/actual processed files and file-level metadata; inspect four user-supplied alternatives, retaining failed download attempts.
- [x] Add explicit round promotion using path/hash/source-bound decisions; preserve old audit as a sealed history file and all prior result attempts. Keep the historical QID builder from overwriting an applied audit.
- [x] Merge only new passed images, normalize their creator attribution, preserve all 387 accepted records/bytes, and rebuild current coverage/report.
- [x] Add binding/history regression checks and extend isolated browser cases to newly recovered Cities plus an unresolved City and both new POIs.
- [x] Run image, license, duplicate, report, retry and browser verifiers; record this run separately from the earlier comprehensive FAIL. Do not rerun performance.
- [ ] Stage only new accepted WebP files, finalize text, generate strict image baseline last, verify protection hashes/index/stash, and stop uncommitted.

Resume implementation complete: 11 new accepted, 45 remaining. Baseline generation and final read-only checks deliberately run after this final text update; terminal artifacts capture their actual outcomes.

## Remaining 45 continuation — 2026-09-09

Execute inline under the same Recovery 02 boundaries. Entry: 398 accepted, 45 debts, 399 staged entries, expected branch/HEAD/stash unchanged; accepted byte hashes verified and pre-edit text snapshotted outside the repository.

- [x] Verify six priority file metadata records; normalize only confirmed same-license canonical URLs and transcribe source-authored credit.
- [x] One bounded Commons search per remaining entity (five hits), at most one rate-limited retry; retain all responses/errors and select only exact photographs.
- [x] Review original/processed files and bind decisions to source and processed hashes.
- [x] Generalize round attempt identity and preserve previous selected/rejected histories; merge only passed assets.
- [x] Rebuild affected manifests/reports, run image/license/history and targeted browser checks, stage only passed WebP, finalize text then generate/verify baseline. Preserve comprehensive FAIL historical result and all protected content.

Continuation result: eight candidates reviewed, three accepted, five rejected. Recovery 02 now has 401 accepted assets and 42 unresolved debts (12 City / 30 Core POI). The Commons search was bounded and rate-limited; remaining records are explicitly not marked exhausted.
