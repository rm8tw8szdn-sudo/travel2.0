export const BATCH03_OUTCOMES=new Set(["ELIGIBLE","SOURCE_NOT_FOUND","FACT_INSUFFICIENT","WRONG_DIRECTION","OTHER_EXPLICIT_BLOCKER","NOT_RESEARCHED"]);
export function validateBatch03ResearchAttempt(record,expectedId){const a=record?.searchAttempt,reasons=[];if(record?.unitIdentity!==expectedId||a?.unitIdentity!==expectedId)reasons.push("unit-mismatch");if(a?.executionStatus!=="EXECUTED")reasons.push("not-executed");if(!Array.isArray(a?.executedQueries)||!a.executedQueries.length)reasons.push("query-missing");if(!a?.searchMethod||a.searchMethod.includes("generated query only"))reasons.push("method-invalid");if(!Array.isArray(a?.checkedResultReferences)||!a.checkedResultReferences.length||a.checkedResultReferences.some(x=>!/^https:\/\//u.test(x?.url||"")||!x?.reviewOutcome))reasons.push("references-missing");if(!a?.sourceAuthorityAssessment||!a?.reviewedSourceFact||!a?.unitSpecificAdjudication)reasons.push("review-incomplete");if(!Number.isFinite(Date.parse(a?.reviewedAt)))reasons.push("time-invalid");if(!BATCH03_OUTCOMES.has(a?.finalResearchOutcome)||a.finalResearchOutcome==="NOT_RESEARCHED")reasons.push("outcome-invalid");if(record?.researchOutcome!==a?.finalResearchOutcome)reasons.push("outcome-mismatch");if(a?.finalResearchOutcome==="SOURCE_NOT_FOUND"&&a.checkedResultReferences.length===0)reasons.push("not-found-without-search");return {accepted:reasons.length===0,reasons}}
