# PR #30 Remaining P1 Closures Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the two remaining merge-blocking P1 findings without changing any unrelated PR #30 behavior.

**Architecture:** Replace the broad `data` directory exposure with exact browser-runtime data files. Move repository lock ownership and dead-process recovery into a focused module that uses atomic lock creation, validated owner metadata, liveness checks, and a serialized recovery claim.

**Tech Stack:** Node.js 24, CommonJS server boundary code, ES modules, Node test runner, Playwright.

---

### Task 1: Restrict public data files

**Files:**
- Modify: `server-security.js`
- Modify: `tests/server-security.test.mjs`
- Modify: `tests/browser/state-security.spec.cjs`

- [x] Add resolver and real HTTP failures for raw knowledge, reports, image audits, LFS paths, traversal, encoded separators, double encoding, mixed separators, cache, configuration, and server source.
- [x] Replace the `data` directory permission with exact entries for `data/countries-50m.json` and `data/countries.zh.json`.
- [x] Verify legitimate HTML, JS, CSS, assets, vendor files, and the two map data files still return 200.
- [x] Run Node, HTTP, and focused Chromium tests.

### Task 2: Recover locks owned by dead processes

**Files:**
- Create: `src/lib/routes/file-owner-lock.mjs`
- Modify: `src/lib/routes/accepted-repository.mjs`
- Modify: `tests/accepted-repository-concurrency.test.mjs`

- [x] Add tests for metadata validation, live-owner protection, dead-owner recovery, restart recovery, malformed metadata, exception cleanup, competing stale recovery, and the existing A/B/A lost-update scenario.
- [x] Write versioned lock metadata containing PID, OS-observed process-start identity, creation time, and a random owner token; use the identity to detect PID reuse.
- [x] Recover only when validated metadata identifies a process that is definitely dead; treat alive or indeterminate liveness as a conflict.
- [x] Serialize stale recovery with an owned recovery marker and finite atomic takeover state, re-read and revalidate the exact owner before removal, recover a marker left by a dead process, and reacquire the canonical lock atomically.
- [x] Release only when the canonical lock still contains the caller's owner token.
- [x] Run the focused repository tests and broader route checks.

### Task 3: Update evidence and publish the fixes

**Files:**
- Modify: `docs/code-review-2026-09-07.md`
- Modify: `docs/browser-acceptance-2026-09-08.md`

- [x] Update documentation only after the real HTTP and crash/restart tests pass.
- [x] Run Node, Chromium, smoke, audit, constraint, failure-propagation, and four BLOCKED prerequisite checks.
- [x] Confirm a clean diff, commit to the existing PR #30 branch, push it, and verify PR state and mergeability without merging.
